interface:
  display_name: "Expo iOS App Builder"
  short_description: "Build iOS-first Expo apps from PRD through implementation and TestFlight."
  default_prompt: "Use $expo-ios-app-builder to create a new iOS-first Expo Router app, implement PRD feature scope (not scaffold-only placeholders), enforce PRD traceability with validator gates, and prepare EAS/TestFlight delivery."
